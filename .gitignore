# --- Clé privée Sparkle : NE JAMAIS COMMITER (signe les mises à jour) ---
*private-key*
*sparkle*key*.txt

# --- Dépendances vendues (téléchargées au build) ---
vendor/

# --- Python ---
.venv/
__pycache__/
*.pyc

# --- Build artefacts ---
build/
FaceKey.app/
.build-cache/
sdk/.build/
outputs/
*.bundle/
pam/pam_faceid.so
helpers/touchid-helper
helpers/auth-modal
helpers/action-modal
helpers/faceid-hud
helpers/builtin-camera

# --- Assets générés (régénérés au build) ---
assets/faceid-icon.png
assets/faceid-icon.icns
assets/appicon-1024.png
assets/FaceKey.icns
i18n/

# --- Données locales / temporaires ---
*.log
.DS_Store

# --- Expérimental : déverrouillage écran (non supporté, cf. LOCK-SCREEN-PLAN.md) ---
# Bloqué par macOS (SIP + restrictions Apple). Gardé en local, hors du dépôt public.
authplugin/
scripts/build-authplugin.sh
scripts/screensaver-*.sh
scripts/pam-screensaver-*.sh
scripts/authplugin-*.sh
scripts/test-camera-locked.sh

# --- Legacy : remplacé par l'app (login item via SMAppService, caméra via le bundle) ---
launchagent/
scripts/install-agent.sh
scripts/fix-camera-launchd.sh

# --- Packaging autonome (PyInstaller) ---
dist/
packaging/dist/
packaging/build/
# packaging/faceid.spec n'est plus ignoré : il était régénéré puis jeté à chaque build,
# c'est maintenant un fichier source que PyInstaller lit tel quel. L'ignorer le rendait
# absent de tout clone neuf, et le build échouait sur « Spec file not found ».

# Frames intermédiaires du GIF de démo (on garde assets/demo.gif)
assets/demo_frames/

# Concepts de logo (exploration)
assets/logos/

# python.org CPython framework (provisioned by scripts/setup-python.sh)
.python/
.venv.homebrew-backup/
.python-pkg/
packaging/pyinstaller.log
